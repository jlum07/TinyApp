<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<header>
  <% include partials/_header %>
</header>
<body>
    <!-- Add your EJS code here -->
    <h1>TinyApp URL Shortener</h1>
      <ul>
        <% for (let key in urls) { %>
          <li>
            <%= `${key} ------------------  ${urls[key]}` %>
          </li>
        <% } %>
      </ul>
</body>
</html>